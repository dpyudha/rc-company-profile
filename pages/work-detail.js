import React from 'react'
import { connect } from 'react-redux'


class WorkDetail extends React.Component {
  static async getInitialProps (props) {
   
  }

  componentDidMount () {
  }

  render () {
    return <div></div>
  }
}

export default connect()(WorkDetail)
